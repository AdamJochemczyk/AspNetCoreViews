@model zaj9.Models.Lab

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Lab</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IDLAB" />
            <div class="form-group">
                <label asp-for="NazwaLab" class="control-label"></label>
                <input asp-for="NazwaLab" class="form-control" />
                <span asp-validation-for="NazwaLab" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="iloscPracownikow" class="control-label"></label>
                <input asp-for="iloscPracownikow" class="form-control" />
                <span asp-validation-for="iloscPracownikow" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
